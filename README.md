# `goon.vc`
Source code for the G00N website.

## Quick Start
All static assets are served from the `assets/` directory.

### Compiling
Use `npm run build` to re-build the static assets.

### Running
Use `npm start` to compile assets and start the node.
